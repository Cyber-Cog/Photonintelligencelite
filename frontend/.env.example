# Local Vite / Docker Compose
VITE_API_BASE_URL=http://localhost:8000

# Vercel (production): set in the Vercel project Environment Variables dashboard
# (not this file). Value = Render API origin, no trailing slash, e.g.:
# VITE_API_BASE_URL=https://pic-lite-api.onrender.com
