/**
 * Backward-compatible export — app shell lives in AppLayout.
 * Prefer importing from `@/components/AppLayout` for new code.
 */
export { AppLayout as Layout, AppLayout, TopNavbar, MainContent } from "@/components/AppLayout";
